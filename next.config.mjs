/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['elosiubafavour.vercel.app'], // Add your domain here
  },
};

export default nextConfig;
